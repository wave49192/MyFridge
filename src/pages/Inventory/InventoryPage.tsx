import React from "react";
import { Navbar } from "../../components";

const InventoryPage: React.FC = () => {
  return (
    <div>
      <Navbar isAtPage="Inventory" />
    </div>
  );
};

export default InventoryPage;
